spring.application.name=${APP_NAME:cloudcart-product-service}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/cloudcart}
spring.datasource.username=${DB_USERNAME:cloudcart}
spring.datasource.password=${DB_PASSWORD:cloudcart123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
